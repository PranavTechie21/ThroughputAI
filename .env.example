# Example environment configuration for MongoDB Atlas
# Copy this to .env and update with your actual values

# Frontend API URL
VITE_API_URL=http://localhost:3000

# MongoDB Atlas Configuration
# Replace the values below with your actual MongoDB Atlas credentials
# Example: mongodb+srv://myuser:mypassword@cluster0.abc123.mongodb.net/railway_dashboard?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/railway_dashboard?retryWrites=true&w=majority

# JWT Secret for authentication (use a strong, random secret in production)
JWT_SECRET=your-super-secure-jwt-secret-key-change-this-in-production

# Server Port
PORT=5000

# Node Environment
NODE_ENV=development

# MongoDB Atlas Setup Instructions:
# 1. Go to https://cloud.mongodb.com/
# 2. Create a new account or sign in
# 3. Create a new cluster (free tier available)
# 4. Create a database user with read/write permissions
# 5. Whitelist your IP address (or use 0.0.0.0/0 for development)
# 6. Get your connection string from "Connect" > "Connect your application"
# 7. Replace <username>, <password>, and cluster URL in MONGO_URI above